import 'package:flutter/material.dart';

class ResearcherPage extends StatelessWidget {
  const ResearcherPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Researcher Dashboard'),
      ),
      body: const Center(
        child: Text('Researcher Dashboard - Coming Soon'),
      ),
    );
  }
} 